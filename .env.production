# Fork-only (kanapals prod) build-time branding. NEXT_PUBLIC_* values are
# inlined by `next build`. The chat sender prefix keeps web sends reading as
# 'Kanrinin: <msg>' in-game, matching palcon (upstream default is '[Admin] ').
NEXT_PUBLIC_CHAT_SENDER_PREFIX="Kanrinin: "
